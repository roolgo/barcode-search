# Barcode-Search
Search products with barcodes using Cosmos Bluesoft API
Define your token API in the file `process.env` with the variable name `REACT_APP_TOKEN_API`
>Note: `REACT_APP_TOKEN_API` will be exposed in your JS as `process.env.REACT_APP_TOKEN_API`.




